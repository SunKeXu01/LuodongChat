PORT=8787
UPSTREAM_BASE_URL=https://api.openai.com
UPSTREAM_RESPONSES_PATH=/v1/responses
UPSTREAM_API_KEY=replace-me
# Optional server-side model override. This keeps the desktop client independent
# from provider-specific model names (for example qwen, kimi, mimo, or deepseek).
# The endpoint must expose an OpenAI Responses-compatible API.
# UPSTREAM_MODEL=
# UPSTREAM_SUPPORTS_WEB_SEARCH=false
# UPSTREAM_SUPPORTS_IMAGE_GENERATION=false
# Optional comma-separated credential pool. Falls back to UPSTREAM_API_KEY.
# UPSTREAM_API_KEYS=replace-me-1,replace-me-2
# Optional JSON array for credentials hosted at different upstream URLs.
# UPSTREAM_ENDPOINTS_JSON=[{"baseUrl":"https://another.example/v1","responsesPath":"/responses","apiKey":"replace-me","model":"provider-model-id","supportsWebSearch":false,"supportsImageGeneration":false}]
# Comma-separated SHA-256 hashes of user gateway keys. Never store plaintext keys here.
GATEWAY_KEY_HASHES=
# Optional SHA-256 hashes for the read-only /admin dashboard. Never reuse a gateway key.
ADMIN_KEY_HASHES=
REQUESTS_PER_MINUTE=30
MAX_CONCURRENT_REQUESTS=2
UPSTREAM_TIMEOUT_MS=300000
POSTGRES_PASSWORD=replace-with-a-long-random-password
DATABASE_URL=postgresql://connector:replace-me@127.0.0.1:5432/connector
REDIS_URL=redis://127.0.0.1:6379
# Optional email-verified self-service enrollment. Secrets stay in the server .env only.
# Issued keys are permanent and have no daily request quota.
SELF_SERVICE_ENABLED=false
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USERNAME=
SMTP_AUTH_CODE=
SMTP_FROM=
SELF_SERVICE_REQUESTS_PER_MINUTE=30
SELF_SERVICE_MAX_CONCURRENT=2
