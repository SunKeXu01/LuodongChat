<Window x:Class="ChatGPTConnector.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChatGPT 连接器" Width="520" Height="700" MinWidth="380" MinHeight="480"
        Icon="/ChatGPTConnector;component/Assets/AppIcon.png"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">
  <Grid Margin="30,24">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <TextBlock Text="ChatGPT 连接器" FontSize="26" FontWeight="SemiBold"/>

    <StackPanel x:Name="LoginPanel" Grid.Row="1" VerticalAlignment="Center">
      <TextBlock Text="登录或注册" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
      <TextBlock Text="使用邮箱验证码登录；首次验证将自动创建账户。" Foreground="#6B7280" TextAlignment="Center" Margin="0,8,0,24" TextWrapping="Wrap"/>
      <TextBlock Text="邮箱" Margin="0,0,0,6"/>
      <TextBox x:Name="LoginEmailInput" Height="40" Padding="10,7"/>
      <Grid Margin="0,12,0,0">
        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="125"/></Grid.ColumnDefinitions>
        <TextBox x:Name="LoginCodeInput" Height="40" Padding="10,7" MaxLength="6"/>
        <Button x:Name="LoginCodeButton" Grid.Column="1" Content="获取验证码" Margin="10,0,0,0" Click="LoginCodeButton_OnClick"/>
      </Grid>
      <Button x:Name="LoginButton" Content="登录 / 注册" Height="46" FontSize="16" Margin="0,14,0,0" Click="LoginButton_OnClick"/>
      <TextBlock x:Name="LoginNotice" Foreground="#047857" TextAlignment="Center" TextWrapping="Wrap" Margin="0,12,0,0"/>
    </StackPanel>

    <TabControl x:Name="AccountPanel" Grid.Row="1" Margin="0,20,0,0" Visibility="Collapsed">
      <TabItem Header="首页">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="18,24">
            <Ellipse x:Name="StatusDot" Width="18" Height="18" Fill="#9CA3AF" HorizontalAlignment="Center"/>
            <TextBlock x:Name="StatusText" Text="正在建立本地连接…" FontSize="18" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,12,0,8"/>
            <TextBlock Text="AI 模型：GPT-5.6" FontWeight="SemiBold" TextAlignment="Center" Foreground="#374151" Margin="0,0,0,24"/>
            <Border Background="#F3F4F6" CornerRadius="8" Padding="14">
              <TextBlock Text="保持连接器运行并处于登录状态，Codex 才能使用此连接。退出登录或退出软件后将自动恢复原始 Codex 环境。" TextWrapping="Wrap" Foreground="#4B5563"/>
            </Border>
            <Button x:Name="ReconnectButton" Content="重新建立连接" Height="46" Margin="0,18,0,0" Click="ReconnectButton_OnClick"/>
            <Button x:Name="UpdateButton" Content="检查客户端更新" Height="42" Margin="0,12,0,0" Click="UpdateButton_OnClick"/>
            <TextBlock x:Name="UpdateText" Foreground="#047857" TextAlignment="Center" Margin="0,8,0,0"/>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
      <TabItem Header="聊天">
        <Grid Margin="14,18">
          <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
          <DockPanel>
            <TextBlock Text="GPT-5.6" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button x:Name="NewChatButton" Content="新建会话" HorizontalAlignment="Right" Padding="14,6" Click="NewChatButton_OnClick"/>
          </DockPanel>
          <ListBox x:Name="ChatMessagesList" Grid.Row="1" Margin="0,12" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Border Background="#F3F4F6" CornerRadius="8" Padding="12" Margin="2,4" MaxWidth="390" HorizontalAlignment="Stretch">
                  <StackPanel>
                    <TextBlock Text="{Binding Sender}" FontWeight="SemiBold" Foreground="#0F766E"/>
                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
          <TextBlock x:Name="ChatNotice" Grid.Row="2" Foreground="#B91C1C" TextWrapping="Wrap" Margin="2,0,2,6"/>
          <Grid Grid.Row="3">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="82"/></Grid.ColumnDefinitions>
            <TextBox x:Name="ChatInput" MinHeight="44" MaxHeight="110" Padding="10,8" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            <Button x:Name="ChatSendButton" Grid.Column="1" Content="发送" Margin="10,0,0,0" Click="ChatSendButton_OnClick"/>
          </Grid>
        </Grid>
      </TabItem>
      <TabItem Header="个人">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="20,24">
            <Border Width="104" Height="104" CornerRadius="52" BorderBrush="#D1D5DB" BorderThickness="1" HorizontalAlignment="Center" ClipToBounds="True">
              <Image x:Name="AvatarImage" Stretch="UniformToFill"/>
            </Border>
            <Button x:Name="UploadAvatarButton" Content="上传头像" Width="110" Margin="0,10,0,22" HorizontalAlignment="Center" Click="UploadAvatarButton_OnClick"/>
            <TextBlock Text="邮箱" Foreground="#6B7280"/>
            <TextBlock x:Name="ProfileEmailText" FontSize="16" Margin="0,4,0,16" TextWrapping="Wrap"/>
            <TextBlock Text="网名" Foreground="#6B7280"/>
            <TextBox x:Name="NicknameInput" Height="40" Padding="10,7" MaxLength="20" Margin="0,5,0,0"/>
            <Button x:Name="SaveProfileButton" Content="保存个人资料" Height="42" Margin="0,12,0,0" Click="SaveProfileButton_OnClick"/>
            <Button x:Name="LogoutButton" Content="退出登录" Height="42" Margin="0,28,0,0" Foreground="#B91C1C" Click="LogoutButton_OnClick"/>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
      <TabItem Header="消费">
        <StackPanel Margin="20,28">
          <TextBlock Text="账户余额" Foreground="#6B7280"/>
          <TextBlock x:Name="BalanceText" Text="¥0.00" FontSize="32" FontWeight="SemiBold" Margin="0,5,0,18"/>
          <Border Background="#FFF7ED" CornerRadius="8" Padding="14"><TextBlock Text="消费流水功能正在建设中，正式启用计费前不会扣除余额。" Foreground="#9A3412" TextWrapping="Wrap"/></Border>
        </StackPanel>
      </TabItem>
    </TabControl>

    <StackPanel Grid.Row="2" Margin="0,18,0,0">
      <TextBlock Text="客服：2554798585（QQ）" Foreground="#6B7280"/>
      <TextBlock Text="网关：https://520skx.com" Foreground="#6B7280" Margin="0,3,0,0"/>
    </StackPanel>
  </Grid>
</Window>
