<Application x:Class="ChatGPTConnector.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="PageBackgroundBrush" Color="#F4F7FA"/>
    <SolidColorBrush x:Key="TopBarBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BrandBrush" Color="#142033"/>
    <SolidColorBrush x:Key="BrandHoverBrush" Color="#1D2B40"/>
    <SolidColorBrush x:Key="BrandForegroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#12B8A6"/>
    <SolidColorBrush x:Key="TextBrush" Color="#111827"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#667085"/>
    <SolidColorBrush x:Key="WeakTextBrush" Color="#98A2B3"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#DCE3EA"/>
    <SolidColorBrush x:Key="LinkBrush" Color="#078575"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#B42318"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#B54708"/>
    <SolidColorBrush x:Key="SecondarySurfaceBrush" Color="#EAF8F6"/>
    <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="#08786C"/>
    <SolidColorBrush x:Key="SidebarBrush" Color="#F7F7F8"/>
    <SolidColorBrush x:Key="MessageSurfaceBrush" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="UserMessageBrush" Color="#E8F2FF"/>
    <SolidColorBrush x:Key="UserMessageBorderBrush" Color="#C7DCFA"/>
    <SolidColorBrush x:Key="VersionPillBrush" Color="#EEF3F8"/>
    <SolidColorBrush x:Key="VersionPillTextBrush" Color="#08786C"/>
    <SolidColorBrush x:Key="CurrentVersionPillBrush" Color="#EAF8F6"/>
    <SolidColorBrush x:Key="UpdateVersionPillBrush" Color="#FEE4E2"/>
    <SolidColorBrush x:Key="UpdateVersionPillTextBrush" Color="#B42318"/>
    <SolidColorBrush x:Key="ThemeButtonBrush" Color="#F1F4F8"/>
    <SolidColorBrush x:Key="ThemeIconBrush" Color="#475467"/>
    <SolidColorBrush x:Key="LeftPanelBrush" Color="#142033"/>
    <SolidColorBrush x:Key="DangerSurfaceBrush" Color="#FEE4E2"/>
    <SolidColorBrush x:Key="DangerTextBrush" Color="#B42318"/>
    <Style TargetType="Window">
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
      <Setter Property="Background" Value="{DynamicResource PageBackgroundBrush}"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="MinHeight" Value="46"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Style.Triggers><Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter Property="BorderThickness" Value="2"/></Trigger></Style.Triggers>
    </Style>
    <Style x:Key="WatermarkTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <Grid>
                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                <TextBlock x:Name="Watermark" Text="{TemplateBinding Tag}" Foreground="{DynamicResource WeakTextBrush}"
                           Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
              </Grid>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="Text" Value=""><Setter TargetName="Watermark" Property="Visibility" Value="Visible"/></Trigger>
              <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/><Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter TargetName="InputBorder" Property="BorderThickness" Value="2"/></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="InputBorder" Property="Opacity" Value="0.6"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="PasswordBox">
      <Setter Property="MinHeight" Value="46"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Style.Triggers><Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter Property="BorderThickness" Value="2"/></Trigger></Style.Triggers>
    </Style>
    <Style TargetType="Button">
      <Setter Property="MinHeight" Value="40"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Background" Value="{DynamicResource BrandBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource BrandForegroundBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.88"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.72"/></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.55"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{DynamicResource SecondarySurfaceBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
    </Style>
    <Style x:Key="LinkButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="MinHeight" Value="24"/><Setter Property="Padding" Value="4,2"/><Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource LinkBrush}"/>
    </Style>
    <Style x:Key="InlineButton" TargetType="Button" BasedOn="{StaticResource LinkButton}">
      <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/><Setter Property="FontSize" Value="12"/>
    </Style>
    <Style x:Key="HeaderButton" TargetType="Button" BasedOn="{StaticResource LinkButton}">
      <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/><Setter Property="Padding" Value="10,6"/>
    </Style>
    <Style x:Key="VersionBadgeButton" TargetType="Button">
      <Setter Property="Height" Value="24"/><Setter Property="MinHeight" Value="24"/><Setter Property="Padding" Value="9,0"/>
      <Setter Property="Background" Value="{DynamicResource UpdateVersionPillBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource UpdateVersionPillTextBrush}"/>
      <Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
        <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="999" Padding="{TemplateBinding Padding}">
          <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.82"/></Trigger>
          <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.68"/></Trigger>
          <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter TargetName="Chrome" Property="BorderThickness" Value="2"/></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="FeatureTag" TargetType="Border">
      <Setter Property="Background" Value="#22344C"/><Setter Property="CornerRadius" Value="12"/>
      <Setter Property="Padding" Value="12,7"/><Setter Property="Margin" Value="0,0,8,8"/>
      <Setter Property="TextElement.Foreground" Value="#DCE6F1"/><Setter Property="TextElement.FontSize" Value="12"/>
    </Style>
    <Style x:Key="FieldLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Margin" Value="0,12,0,6"/>
    </Style>
    <Style x:Key="FieldError" TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/><Setter Property="FontSize" Value="12"/><Setter Property="Margin" Value="2,5,0,0"/><Setter Property="TextWrapping" Value="Wrap"/></Style>
    <Style x:Key="FormError" TargetType="TextBlock" BasedOn="{StaticResource FieldError}"><Setter Property="Margin" Value="2,12,0,0"/></Style>
    <Style TargetType="TabItem">
      <Setter Property="Padding" Value="15,9"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
      <Setter Property="Margin" Value="0,0,4,0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="TabChrome" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabChrome" Property="Background" Value="{DynamicResource SecondarySurfaceBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TabChrome" Property="Opacity" Value="0.78"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="VersionPill" TargetType="Border">
      <Setter Property="Height" Value="24"/><Setter Property="Padding" Value="9,0"/>
      <Setter Property="CornerRadius" Value="999"/><Setter Property="Background" Value="{DynamicResource CurrentVersionPillBrush}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource VersionPillTextBrush}"/>
      <Setter Property="TextElement.FontSize" Value="12"/><Setter Property="TextElement.FontWeight" Value="Medium"/>
    </Style>
    <Style x:Key="ThemeToggleButton" TargetType="Button">
      <Setter Property="Width" Value="36"/><Setter Property="Height" Value="36"/><Setter Property="MinHeight" Value="36"/>
      <Setter Property="Padding" Value="8"/><Setter Property="Background" Value="{DynamicResource ThemeButtonBrush}"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeIconBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/><Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ThemeChrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThemeChrome" Property="Opacity" Value="0.82"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="ThemeChrome" Property="Opacity" Value="0.68"/></Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ThemeChrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter TargetName="ThemeChrome" Property="BorderThickness" Value="2"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Application.Resources>
</Application>
