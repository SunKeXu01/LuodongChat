<Window x:Class="ChatGPTConnector.App.LogoutConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="退出登录" Width="460" Height="250" WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource PageBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
  <Grid Margin="30">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <TextBlock Text="退出登录？" FontSize="25" FontWeight="SemiBold"/>
    <TextBlock Grid.Row="1" Text="退出后，需要重新登录才能使用泺栋 Chat。" Margin="0,14,0,0" FontSize="14" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap"/>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="取消" MinWidth="78" Height="40" Style="{StaticResource OutlinedButton}" IsCancel="True"/>
      <Button Content="退出登录" MinWidth="94" Height="40" Margin="10,0,0,0" Background="{DynamicResource DangerTextBrush}" Foreground="White" IsDefault="True" Click="ConfirmButton_OnClick"/>
    </StackPanel>
  </Grid>
</Window>
